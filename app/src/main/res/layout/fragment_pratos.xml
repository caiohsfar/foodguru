<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Prato.GUI.PratosFragment">

    <!-- TODO: Update blank fragment layout -->

    <Spinner
        android:id="@+id/spinnerSessao"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:paddingLeft="45dp"
        android:foreground="@drawable/gradient_spinner"
        style="@style/Widget.AppCompat.Spinner.Underlined"/>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <android.support.v7.widget.RecyclerView
            android:id="@+id/recyclerv_view_pratos"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="50dp"
            tools:listitem="@layout/prato_list_item" />
    </RelativeLayout>
    <ProgressBar
        android:id="@+id/progress_bar_pratos"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_gravity="center"
        android:progressDrawable="@color/VerdeFoodGuru" />


</FrameLayout>