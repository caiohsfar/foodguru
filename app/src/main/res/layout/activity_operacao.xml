<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".cliente.GUI.OperacaoActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbarOperacao"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:background="@color/LaranjaFoodGuru"
        android:gravity="center"
        android:elevation="4dp"
        android:foregroundTint="@color/Branco"
        >
        <ImageView
            android:layout_width="200dp"
            android:layout_height="56dp"
            app:srcCompat="@drawable/ic_food_guru"
            android:scaleType="centerCrop"/>
    </android.support.v7.widget.Toolbar>

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_below="@id/toolbarOperacao">

        <FrameLayout
            android:id="@+id/frame_operacao"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            tools:layout_editor_absoluteX="8dp"
            tools:layout_editor_absoluteY="8dp"
            android:layout_above="@id/nav_operacao">
        </FrameLayout>

        <android.support.design.widget.BottomNavigationView
            android:id="@+id/nav_operacao"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?android:attr/windowBackground"
            android:layout_alignParentBottom="true"
            app:menu="@menu/menu_operacao" />
    </RelativeLayout>

</RelativeLayout>